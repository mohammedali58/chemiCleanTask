namespace ChemiClean.SharedKernel

{
    public class SharedKernelEnums
    {
        public enum SortDirection
        {
            Ascending = 0,
            Descending = 1
        }

        public enum Paging
        {
            DefaultPageNumber = 1,
            DefaultPageSize = 10
        }

       
    }
}